backend>
Windows (CMD):
python -m venv venv
venv\Scripts\activate
uvicorn app:app --reload
pip freeze > requirements.txt

**print() will bring up printer in Electron
**check correct address
git remote -v


Open the Command Palette in VS Code (Ctrl+Shift+P).
Select Python: Select Interpreter.
Choose your project's virtual environment (e.g., something like .venv\Scripts\python.exe).

import logging

logging.basicConfig(level=logging.INFO)  
logger = logging.getLogger(__name__)


const { user } = useAuth();

if (user?.role === "admin") {
  // Show admin page
}

shell process:
cd backend
venv\Scripts\activate (if not already)

cd backend
uvicorn app:app --reload

project-tracker
npm run dev

