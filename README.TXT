backend>
Windows (CMD):
python -m venv venv
venv\Scripts\activate
uvicorn app:app --reload
pip freeze > requirements.txt

**print() will bring up printer in Electron
**check correct address
git remote -v
